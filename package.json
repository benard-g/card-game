{
  "name": "card-game",
  "version": "0.1.0",
  "author": "Guillaume Bénard <guillaume@benard.io>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "wsrun -at build",
    "build:clean": "wsrun -a build:clean",
    "test": "wsrun -a test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
