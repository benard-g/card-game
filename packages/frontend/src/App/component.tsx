import React, { FC } from 'react';

import Routes from './Routes';
import { Root } from './styles';

const App: FC = () => {
  return (
    <Root>
      <Routes />
    </Root>
  );
};

export default App;
